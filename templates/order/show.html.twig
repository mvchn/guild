{% extends 'base.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <h1>{{ 'Order'|trans }} {{ order.id }}</h1>
                <br>
                <p>{{ order.status }}</p>
            </div>
        </div>
    </div>

{% endblock %}